
Replace files: "mpkMouseControl.cpp", "mpkMouseControl.H" and "mpk_opt.cpp" 
in the "gui" directory with the new files. Then you would also need to do 
"#define MPK_OPTIMIZE_IMPLEMENTED" either in the Make.config or in the windows
settings or in the code itself. 

